# experiments/04-rag-grounding — RAG Grounding A/B Demo

> **The first experiment that composes Sauti's three-layer memory.** Text question → MiniLM retrieves top-3 chunks from `knowledge.db` → assembled prompt (§ 4.5 verbatim) → Qwen3 / Gemma3 → grounded English answer. The Inspector exposes a `Disable RAG For Comparison` toggle so the same question runs twice — with and without retrieval — and you can see the answer change.

---

## What this experiment proves

1. `SautiRag` (MEM-002) loads `knowledge.db` from `StreamingAssets/VoiceAI/rag/` and returns the top-K most-similar chunks for any English query.
2. The § 4.5 prompt assembly works end-to-end: system rules (§ 9) + `TemporaryMemory.BuildPromptBlock()` (Layer 2) + RAG context (Layer 3) + user question.
3. LLMUnity consumes the assembled prompt and streams a grounded answer that uses facts not in its training data (e.g. "Elder Maren only speaks after dark" is in the Frostmere knowledge base, not in Qwen3's weights).
4. **The grounding actually changes the answer.** Toggle off — generic answer. Toggle on — Frostmere-canon answer.

## Why this demo "proves" RAG works

A common failure mode in RAG demos: the LLM "knew the answer anyway" from training, so the chunks made no observable difference. To avoid that, the Session-4 Frostmere knowledge base is **net-new fiction** — Elder Maren, Captain Thorne, the Crystal Caverns, the Stormwall harbour, the Seep magic system. Qwen3 cannot have seen any of it.

Run the experiment twice:
1. **`disableRagForComparison = true`.** Ask: "Who guards the artifact in the Crystal Caverns?" Expected answer: generic / hedging / "I don't know."
2. **`disableRagForComparison = false`.** Same question. Expected answer: references Elder Maren and the after-dark constraint, drawn from `knowledge-base/npcs/elder-maren.md` via retrieval.

If both answers look similar, retrieval is not firing — check `OnRetrievedChunks` debug panel.

## Prerequisites

| Item | Where |
|---|---|
| Unity 6+ LTS | Install via Unity Hub. |
| `all-MiniLM-L6-v2-int8.onnx` + WordPiece `vocab.txt` | Download per `ai-models/embeddings/manifest.json`. Tracked as `MINILM-DL-001`. |
| `qwen3-1.7b-q5_k_m.gguf` (or `gemma3-1b-q4_k_m.gguf` on Quest) | Per `ai-models/llm/manifest.json`. Tracked as `QWEN-DL-001` / `GEMMA-DL-001`. |
| `knowledge.db` | Generated by **Sauti → Build Knowledge Base** Editor menu (MEM-003) once the embedder lands (`RAG-EMB-API-001`). |
| LLMUnity API binding | `LLM-API-001`. |
| LLMUnity RAG load/search binding | `RAG-API-001`. |

Until all five of those resolve, the scaffold logs intent only — see the `NEEDS_VERIFICATION` regions in `RagGroundedAsk.cs`.

## How to run

1. Open the repo root as a Unity project in Unity Hub.
2. Wait for package import.
3. Create `GroundedScene.unity` per the steps in `GroundedScene.unity.placeholder.md`.
4. Press **Play**.
5. With `Disable RAG For Comparison = true`, click **Ask**. Note the answer.
6. Toggle `Disable RAG For Comparison = false`. Click **Ask**. Compare.

## Expected behaviour

- On Awake, the script instantiates `SautiRag` (default ctor → `LlmUnityRagBackend`) and calls `LoadAsync(StreamingAssetsPath/VoiceAI/rag/knowledge.db)`. If load fails (model missing, RAG-API-001 unverified), the script disables itself and logs the cause.
- `Ask()` retrieves top-K chunks via `SautiRag.SearchAsync(question, numResults)`, assembles the prompt per § 4.5 verbatim, streams the LLM response, and fires:
  - `OnRetrievedChunks(chunks[])` — debug visibility into what retrieval surfaced
  - `OnGroundedAnswer(full)` — the final response once the stream completes
- When `disableRagForComparison = true`, the RAG retrieval is **still performed** (so the chunks are visible in the debug panel) but the chunks are **omitted from the LLM prompt**. The toggle isolates retrieval from grounding for the A/B.

## Known limitations (Session 11 scaffold)

- All five upstream dependencies (`MINILM-DL-001`, `QWEN-DL-001`, `RAG-API-001`, `RAG-EMB-API-001`, `LLM-API-001`) are still open — the scaffold cannot actually run end-to-end yet. Tracked as `RAG-DEMO-001`.
- Layer 1 conversation history is not used here (single-shot Q&A). EXP-005 (full voice loop) wires it up via `LLMUnity.LLMAgent.AIHeroHistory = 10`.
- Layer 2 `TemporaryMemory` is wired but expected to be empty for a fresh demo session. The README later examples show how to seed it.
- No score-threshold gating yet — `RAG-003` in `memory/todo.md § 3.12` covers that polish.

## Files in this experiment

| File | Purpose |
|---|---|
| `README.md` | This file. |
| `RagGroundedAsk.cs` | The MonoBehaviour scaffold. |
| `GroundedScene.unity.placeholder.md` | Instructions to create the scene by hand. |

## Cross-references

- Spec: `memory/voice_ai_architecture.md § 4.3` (RAG), `§ 4.5` (prompt assembly verbatim).
- Composed subsystems: `Assets/Sauti/Runtime/Scripts/SautiRag.cs` (MEM-002) + `TemporaryMemory.cs` (MEM-001).
- Precedent: `experiments/03-llm-chat/LlmChat.cs` (sentence-boundary streaming, model selection, fence pattern).
- Task: `memory/todo.md § 2 EXP-004`, `RAG-DEMO-001`.
